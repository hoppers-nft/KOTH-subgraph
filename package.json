{
  "name": "KOTH-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ kontrol-apa/KOTH-subgraph",
    "create-local": "graph create --node http://localhost:8020/ kontrol-apa/KOTH-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ kontrol-apa/KOTH-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kontrol-apa/KOTH-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.4",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
